# 🏥 Medical Analysis Frontend (Streamlit)

Веб-інтерфейс для системи медичного аналізу з прогнозуванням стану здоров'я пацієнтів.

---

## 📋 Зміст

- [Можливості](#можливості)
- [Технології](#технології)
- [Встановлення](#встановлення)
- [Запуск проекту](#запуск-проекту)
- [Структура проекту](#структура-проекту)
- [Використання](#використання)
- [Docker](#docker)
- [Troubleshooting](#troubleshooting)

---

## 🎯 Можливості

- ✅ **Single Analysis** — аналіз одного пацієнта з формою введення
- ✅ **Batch Analysis** — масовий аналіз з завантаженням CSV
- ✅ **Візуалізація** — gauge charts, radar charts, статус-карти
- ✅ **Референтні норми** — таблиця нормальних значень
- ✅ **Кольорова індикація** — статуси (Healthy/Warning/Moderate Risk/Critical)
- ✅ **Експорт результатів** — завантаження CSV з результатами
- ✅ **Інтеграція з Backend API** — перевірка health check

---

## 🛠 Технології

- **Python 3.10 - 3.13** — підтримувані версії
- **Streamlit 1.39.0** — веб-фреймворк
- **Plotly 5.24.1** — інтерактивні графіки
- **Pandas 2.2.3** — обробка даних (сумісна з Python 3.13)
- **Requests 2.32.3** — HTTP клієнт для Backend API
- **Python-dotenv 1.0.1** — змінні середовища
- **Docker** — контейнеризація

---

## 📦 Встановлення

### 1️⃣ Клонування репозиторію

```bash
git clone https://github.com/yourusername/MedicalDataAnalisys.git
cd MedicalDataAnalisys
git checkout frontend  # Перемикаємося на гілку frontend
```

### 2️⃣ Перехід до папки frontend

```bash
cd frontend
```

### 3️⃣ Створення віртуального середовища

```bash
python3 -m venv .venv
```

### 4️⃣ Активація віртуального середовища

**macOS/Linux:**
```bash
source .venv/bin/activate
```

**Windows:**
```bash
.venv\Scripts\activate
```

### 5️⃣ Оновлення pip

```bash
python -m pip install --upgrade pip
```

### 6️⃣ Встановлення залежностей

```bash
python -m pip install -r requirements.txt
```

### 7️⃣ Створення файлу .env

Файл `.env` вже має бути створений. Якщо ні, скопіюйте з шаблону:

```bash
cp .env.example .env
```

Переконайтеся, що налаштування правильні:

```env
BACKEND_API_URL=http://localhost:8000
API_KEY=my-super-secret-api-key-2024
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
```

---

## 🚀 Запуск проекту

### Варіант 1: Через shell скрипт (рекомендовано)

```bash
cd frontend
./scripts/start_frontend.sh
```

### Варіант 2: Через Streamlit CLI

```bash
cd frontend
source .venv/bin/activate
streamlit run app.py
```

### Варіант 3: З користувацьким портом

```bash
streamlit run app.py --server.port=8502
```

### ✅ Перевірка запуску

Додаток запуститься на `http://localhost:8501`

Відкрийте у браузері або:

```bash
open http://localhost:8501  # macOS
xdg-open http://localhost:8501  # Linux
```

---

## 📁 Структура проекту

```
frontend/
├── app.py                      # Головна сторінка
├── config.py                   # Конфігурація
├── requirements.txt            # Python залежності
├── Dockerfile                  # Docker образ
├── docker-compose.yml          # Docker Compose
├── .env                        # Змінні середовища
├── .env.example                # Шаблон .env
├── .gitignore                  # Git ignore
├── README.md                   # Документація (цей файл)
├── QUICKSTART.md               # Швидкий старт
│
├── pages/                      # Додаткові сторінки
│   ├── 1_📊_Single_Analysis.py # Аналіз одного пацієнта
│   ├── 2_📁_Batch_Analysis.py  # Масовий аналіз
│   └── 3_📈_Statistics.py      # Статистика
│
├── components/                 # UI компоненти
│   ├── __init__.py
│   ├── forms.py                # Форми введення
│   ├── visualizations.py       # Графіки та візуалізації
│   └── tables.py               # Таблиці
│
├── services/                   # Бізнес-логіка
│   ├── __init__.py
│   └── api_client.py           # Backend API клієнт
│
├── utils/                      # Утиліти
│   ├── __init__.py
│   └── formatters.py           # Форматування даних
│
└── scripts/                    # Shell скрипти
    ├── start_frontend.sh       # Запуск frontend
    └── test_connection.sh      # Тест підключення до backend
```

---

## 📖 Використання

### Головна сторінка

Після запуску відкриється головна сторінка з:
- Описом системи
- Статусом backend підключення
- Таблицею референтних норм
- Навігацією до інших сторінок

### 1. Single Analysis (Аналіз одного пацієнта)

1. Перейдіть на сторінку **📊 Single Analysis**
2. Заповніть форму з даними пацієнта:
   - Вік (1-120)
   - Стать (Male/Female)
   - TSH, T3, T4, Інсулін (обов'язкові)
   - Додаткові показники (опціонально)
3. Натисніть **🔍 Провести аналіз**
4. Перегляньте результати:
   - Статус (Healthy/Warning/Moderate Risk/Critical)
   - Gauge chart з ризиком
   - Radar chart з показниками
   - Список аномалій

### 2. Batch Analysis (Масовий аналіз)

1. Перейдіть на сторінку **📁 Batch Analysis**
2. Завантажте CSV файл з даними пацієнтів
3. Переконайтеся, що файл містить колонки:
   - `age`, `gender`, `tsh_level`, `t3_level`, `t4_level`, `insulin`
4. Натисніть **🔍 Аналізувати всіх пацієнтів**
5. Перегляньте результати у таблиці
6. Завантажте результати через кнопку **📥 Завантажити результати (CSV)**

**Приклад CSV:**
```csv
age,gender,tsh_level,t3_level,t4_level,insulin
35,Male,2.5,2.0,8.0,10.0
65,Female,12.5,0.8,3.0,50.0
45,Male,3.2,2.1,9.5,15.3
```

### 3. Statistics (Статистика)

🚧 В розробці

---

## 🐳 Docker

### Запуск через Docker Compose

```bash
cd frontend
docker-compose up --build
```

Додаток буде доступний на `http://localhost:8501`

### Окремий запуск Docker

```bash
cd frontend
docker build -t medical-frontend .
docker run -p 8501:8501 \
  -e BACKEND_API_URL=http://localhost:8000 \
  -e API_KEY=my-super-secret-api-key-2024 \
  medical-frontend
```

### Зупинка Docker

```bash
docker-compose down
```

---

## 🔗 Інтеграція з Backend

Frontend підключається до Backend API за адресою `BACKEND_API_URL` з `.env`.

### Endpoints, які використовуються:

1. **Health Check**
   ```
   GET /health
   ```

2. **Predict**
   ```
   POST /api/v1/predict
   Headers: X-API-Key: your-api-key
   Body: {patient_data}
   ```

### Перевірка підключення

Статус backend відображається в sidebar (ліва панель):
- ✅ **Backend підключено** — все працює
- ❌ **Backend недоступний** — перевірте чи запущений backend

---

## 🎨 Кольорова індикація

Статуси відображаються різними кольорами:

| Статус | Колір | Ризик |
|--------|-------|-------|
| **Healthy** | 🟢 Зелений | 0-30% |
| **Warning** | 🟡 Жовтий | 30-50% |
| **Moderate Risk** | 🟠 Помаранчевий | 50-70% |
| **Critical** | 🔴 Червоний | 70-100% |

---

## 📊 Референтні норми

| Показник | Норма | Одиниці |
|----------|-------|---------|
| TSH | 0.4 - 4.0 | мкМО/мл |
| T3 | 1.3 - 3.1 | нмоль/л |
| T4 | 5.0 - 12.0 | мкг/дл |
| Insulin | 2.6 - 24.9 | мкМО/мл |
| Hemoglobin | 120 - 160 | г/л |
| WBC | 4.0 - 11.0 | 10^9/л |
| RBC | 4.0 - 6.0 | 10^12/л |
| Platelets | 150 - 400 | 10^9/л |

---

## 🐛 Troubleshooting

### ❌ Backend недоступний

```bash
# Перевірте чи запущений backend
curl http://localhost:8000/health

# Якщо ні, запустіть backend
cd ../backend
./start_server.sh
```

### ❌ Порт зайнятий

```bash
# Знайти процес на порту 8501
lsof -ti :8501

# Вбити процес
lsof -ti :8501 | xargs kill -9

# Або запустити на іншому порті
streamlit run app.py --server.port=8502
```

### ❌ Помилка імпорту модулів

```bash
# Перевірте, що знаходитесь у правильній директорії
pwd  # Має бути .../MedicalDataAnalisys/frontend

# Активуйте віртуальне середовище
source .venv/bin/activate

# Переінсталюйте залежності
python -m pip install -r requirements.txt --force-reinstall
```

### ❌ Streamlit не запускається

```bash
# Перевірте версію Python (має бути 3.10+, рекомендовано 3.10-3.12)
python --version

# Перевірте встановлення streamlit
python -m pip show streamlit

# Перевстановіть streamlit
python -m pip install --upgrade streamlit
```

### ❌ Помилка компіляції pandas з Python 3.13

Якщо використовуєте Python 3.13, переконайтеся що використовуєте pandas 2.2.3+:

```bash
# Оновіть requirements.txt до нових версій
python -m pip install --upgrade pandas numpy

# Або перевстановіть всі залежності
python -m pip install -r requirements.txt --force-reinstall --no-cache-dir
```

**Рекомендація:** Для стабільної роботи використовуйте Python 3.10, 3.11 або 3.12.

### ❌ Помилка "Connection refused" до Backend

1. Перевірте `.env` файл — правильна адреса?
2. Запустіть backend на порту 8000
3. Перевірте firewall/антивірус

---

## 🔒 Безпека

### API Key

Усі запити до backend вимагають `X-API-Key` header. Ключ береться з `.env`:

```env
API_KEY=my-super-secret-api-key-2024
```

**⚠️ Не комітьте `.env` файл у Git!**

### CORS

Backend має дозволити запити з `http://localhost:8501`:

```python
# backend/config.py
ALLOWED_ORIGINS=http://localhost:8501,http://localhost:3000
```

---

## 📚 Додаткові ресурси

### Streamlit Документація

- [Streamlit Docs](https://docs.streamlit.io/)
- [Plotly Charts](https://plotly.com/python/)
- [Pandas Guide](https://pandas.pydata.org/docs/)

### Корисні команди

```bash
# Очистити кеш Streamlit
streamlit cache clear

# Запустити з відкриттям у браузері
streamlit run app.py --server.headless=false

# Запустити без автоперезавантаження
streamlit run app.py --server.runOnSave=false
```

---

## 🚀 Наступні кроки (Roadmap)

- [ ] Сторінка Statistics з графіками
- [ ] Історія аналізів пацієнта
- [ ] Порівняння пацієнтів
- [ ] Експорт у PDF
- [ ] Темна тема
- [ ] Багатомовність (UA/EN)
- [ ] Аутентифікація користувачів

---

## 📞 Контакти

- **Репозиторій:** [MedicalDataAnalisys](https://github.com/yourusername/MedicalDataAnalisys)
- **Гілка:** `frontend`
- **Версія:** 1.0.0
- **Python:** 3.10+
- **Streamlit:** 1.29.0

---

## 📄 Ліцензія

MIT License - використовуй для своїх проектів! 🎓

---

## ✅ Quick Start Checklist

- [ ] Склонувати репозиторій
- [ ] Створити `.venv` та активувати
- [ ] Встановити залежності (`pip install -r requirements.txt`)
- [ ] Перевірити `.env` файл
- [ ] Запустити backend (`cd ../backend && ./start_server.sh`)
- [ ] Запустити frontend (`streamlit run app.py`)
- [ ] Відкрити `http://localhost:8501`
- [ ] Перевірити підключення до backend (sidebar)
- [ ] Протестувати Single Analysis
- [ ] Протестувати Batch Analysis

**Готово! 🎉 Frontend працює!**

